# Prevent supply chain attacks by disabling arbitrary lifecycle scripts
ignore-scripts=true

# Enforce deterministic package lockfile
package-lock=true

# Save exact versions to prevent unintended version updates
save-exact=true

# Automatically audit dependencies on installation
audit=true
